---
date: 2023-04-18
description: an introduction to polygon clipping
draft: true
title: polygon clipping
---

import PolygonClipper from '../../components/polygon_clipping/PolygonClipper.svelte';
import EdgePoint from '../../components/polygon_clipping/EdgePoint.svelte';
import PointInPolygon from '../../components/polygon_clipping/PointInPolygon.svelte';
import RayCast from '../../components/polygon_clipping/RayCast.svelte';
import Intersection from '../../components/polygon_clipping/Intersection.svelte';
import colors from '../../lib/colors';

export const triangle = [
  [5, 2],
  [8, 8],
  [2, 8],
];

export const subject = [
	[-0.5, -0.5],
	[0.5, -0.5],
	[0.5, 0.5],
	[-0.5, 0.5],
];

export const width = 10;
export const height = 10;

export const cat = [
	[9.5, 4],
	[8.5, 1],
	[7.5, 3],
	[5.5, 2],
	[6.5, 5],
	[3.5, 4],
	[1.5, 5],
	[1.5, 3],
	[2.5, 2],
	[0.5, 3],
	[0.5, 6],
	[1.5, 9],
	[2.5, 7],
	[5.5, 8],
	[6.5, 9],
	[7.5, 6],
	[8.5, 6],
];

## Point on Right Side
<EdgePoint insideColor={colors.pine} outsideColor={colors.gold} lineColor={colors.text} client:visible />

## Point in Polygon
<PointInPolygon width={width} height={height} polygon={triangle} pointX={5} pointY={6} insideColor={colors.pine} outsideColor={colors.gold} lineColor={colors.text} client:visible />

## Line Intersection
<Intersection intersectionColor={colors.gold} lineColor={colors.text} pointColor={colors.pine} client:visible />

## Ray Casting
<RayCast width={width} height={height} polygon={cat} insideColor={colors.pine} outsideColor={colors.gold} polygonColor={colors.text} client:visible />

## Sutherland-Hodgeman Polygon Clipping
<PolygonClipper subjectColor={colors.pine} strokeColor={colors.text} width={10} height={10} subject={cat} clipper={triangle} client:visible />

